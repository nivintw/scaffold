<!--
SPDX-FileCopyrightText: © {{ year }} {{ author_name }}
SPDX-License-Identifier: {{ license }}
-->

# {{ project_name }}

{% if project_description %}{{ project_description }}{% else %}_TODO: one-line description._{% endif %}

## Getting started

```bash
{% if has_python -%}
uv sync                 # create the venv + install dev tooling
{% endif -%}
uvx prek install        # wire up the pre-commit hooks
{% if 'pytest' in test_frameworks %}uv run pytest           # run the Python tests
{% endif %}{% if 'bats' in test_frameworks %}bats tests/             # run the shell tests
{% endif %}```

## Quality gate

This project carries the shared quality spine: prek hooks (git hygiene, gitleaks,
typos, rumdl, SPDX/REUSE headers{% if has_python %}, ruff, ty{% endif %}) that run
identically locally and in CI. Conventional Commits (gitmoji) are enforced at
commit-msg time (`.cz.toml`); releases on `main` are cut automatically by commitizen.

```bash
uvx prek run --all-files   # run every hook on demand
```

A few hooks shell out to **system tools** prek can't bootstrap — CI installs them for
you, but install them locally too (most are in Homebrew): `hawkeye`, `taplo`{% if include_terraform %}, `trivy`{% endif %}{% if has_python %}, `osv-scanner`{% endif %}.
{% if include_terraform %}
## Terraform

```bash
cd terraform
terraform init
terraform plan
```
{% endif %}{% if include_docker %}
## Docker

```bash
docker compose up --build
```
{% endif %}{% if include_helm %}
## Helm

```bash
helm lint helm/{{ project_slug }}
helm template helm/{{ project_slug }}
```
{% endif %}
## License

[{{ license }}](LICENSE) — and [REUSE](https://reuse.software)-compliant.
